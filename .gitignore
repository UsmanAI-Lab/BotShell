# Secrets — never commit these
**/secrets.env
**/.env

# Keys / certs
**/*.key
**/*.pem
**/*.p12

# Tokens / credentials
.sf-tokens.json
.sf-pkce-verifier

# Logs / transcripts
*.jsonl
*.jsonl.lock

# OS junk
.DS_Store
Thumbs.db

# Node deps
**/node_modules/
